{
  "name": "content-factor",
  "version": "1.0.0",
  "description": "The measurable, automated pipeline for content visibility.",
  "main": "pipeline/index.ts",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "start": "pnpm build && pnpm pipeline",
    "capture": "node dist/pipeline/capture.js",
    "transform": "node dist/pipeline/transform.js",
    "publish-content": "node dist/pipeline/publish.js",
    "measure": "node dist/pipeline/measure.js",
    "pipeline": "echo 'Running pipeline...' && pnpm capture && pnpm transform && pnpm publish-content && pnpm measure && echo 'Pipeline complete.'"
  },
  "dependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.9.1"
  }
}
