# Pipeline Todos

Source: `TODO.md` (root)

## Focus
Capture adapters, transform logic, dist compilation, and pipeline quality.

## Items
- Fix side-effect import in `pipeline/transform.ts` with execution guard.
- Replace `any` with strict types in templates and template map.
- Add a testing framework (Vitest) and seed core tests.
- Implement structured logging and replace console usage.
- Add input validation to interactive capture adapters.
- Add watch mode for development (tsc --watch + nodemon).
- Standardize artifact ID format across all captures.
- Add Markdown preview to CLI captures.
- Improve pipeline error messages with actionable context.
- Add progress indicators for long operations.
- Expand metrics collection in `pipeline/measure.ts`.
